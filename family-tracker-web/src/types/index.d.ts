export type Member = {
  id: string;
  name: string;
  latitude: number;
  longitude: number;
  safe: boolean;
};

export type Shelter = {
  id: string;
  name: string;
  latitude: number;
  longitude: number;
};
